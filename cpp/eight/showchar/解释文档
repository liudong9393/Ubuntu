本程序主要要求是：使用混合输入方式输入数字和字符，在执行过程中getchar()读取每一个字符，包括制表符／换行符等；scanf()再去读取数字时自会跳过空格，制表符和换行符．
出现的问题：scanf()函数中两个数字之间加了一个逗号，这样在输入数字的时候需要添加在两个数字之间一个逗号．
alter:更改过后的输入就比较好，在修改之前只能输入一次数据，然后就退出了，这个问题是getchar()函数读取了输入数据最后的换号符，假如输入的是:a 2 3，然后按下回车，看着好像是３个字符，其实在getchar()函数眼中是６个，其中还包括两个空格和换行符，而换行符会让这个程序跳出大循环．



