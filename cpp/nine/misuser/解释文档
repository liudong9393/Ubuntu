程序功能：用来计算两个数字那个是大．
问题：没有相等情况，不过这个程序主要不考虑这个问题；imax函数传参时如果出现错误，如参数类型不错／参数个数不对，在传统的函数声明情况下（形如：int imax();）是不会报错的，依次执行，但是结果在每个设备上不一样．
alter：使用函数原型声明函数（形如：int imax(int n,int m);），这个情况下在使用这个函数的时候会要求传入的函数参数一定要和声明中个数，类型一致．
注意点：一定要使用原型形式传参，保证函数执行正确．
